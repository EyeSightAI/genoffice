# UToOffice 项目实施文档

> 最后更新：2026-09-05
> 本文档是 UToOffice 项目的「唯一事实来源」，记录产品定位、商业模式、技术架构、功能规划、UI 改造清单、卡密系统设计、当前状态与待办。每次重大决策或改动后同步更新本文档，防止上下文丢失。

---

## 一、项目概述

**UToOffice**：fork 自开源项目 GenOffice（genspark-ai/genoffice，Apache-2.0），改名做中文市场的商业付费办公套件。

- **上游**：`genspark-ai/genoffice`（2 天一更，快节奏）
- **Fork**：`EyeSightAI/genoffice`
- **本地**：`C:\Users\YouTo\UToOffice`
- **产品版本**：0.8.0（安装包 `UToOffice Setup 0.8.0.exe`）

**法律边界**：
- `GenOffice`/`Genspark`/`GenTeam` 是 Mainfunc, Inc. 商标，Apache-2.0 第 6 条要求 fork 不得复用 → 必须彻底改名
- 保留 LICENSE、NOTICE（注明 UToOffice 版权 + derivative work based on GenOffice）
- `ee/` 目录（GenOffice Enterprise License 专有协议）**碰都不能碰**

**不可改动（改了会崩）**：
- `@genoffice/*` workspace 包名
- 字体别名 `GenOffice PUA Blank`、`GenOffice Songti SC` 等
- PDF 内部字段名 `GenOfficeStaticFormFills` 等

---

## 二、商业模式

### 核心变现逻辑（三层）

```
┌─────────────────────────────────────────────┐
│ 技术用户  → BYOK（自己配 key）→ 免费            │
│ 非技术用户 → 卡密会员（解锁设计系统）→ 卖 know-how │
│ 想省事的人 → 托管模型（后期做）→ 需先解决合规      │
└─────────────────────────────────────────────┘
```

**核心**：钱主要从「设计系统 / 模板 / 中文规范」赚（know-how，零 API 成本、无合规风险、毛利 100%），不碰「转售 API」（薄利 + 高风险）。

### 核心价值：让 AI 产出「专业设计感」的 PPT

原生 AI 只会「填文字」，没有「设计」概念（每页标题+要点，美化后千篇一律）。我们的价值是三层 know-how：

| 层 | 内容 | 解决什么问题 |
|---|---|---|
| ① 版式库 | 每种内容对应多种版式（要点/对比/时间线/数据/人物…） | 解决「千篇一律」 |
| ② 设计规范 | 配色、字号层次、对齐留白、图标、数据可视化 | 解决「纯文字没美化」 |
| ③ 质检规则 | 溢出/对齐/留白/配色检查 | 保证「每页达标」 |

**关键原则**：设计规范不能只靠「提示词描述」（AI 不严格遵守），要「具体 HTML 模板 + 样式写死 + AI 只选模板填内容」。

### 会员体系

- **不做账号注册/登录**，用卡密激活
- 两层：免费 / 会员
- 永久卡 ¥299-399、年卡 ¥199（用户确认）
- 会员卖「便利」：非会员用描述也能做，会员点标签一键做

---

## 三、技术架构与原则

### 模块化原则（最重要）

**功能像搭积木，少改上游源码**（上游 2 天一更，改得多 bug 就反复出现）：

1. **新增文件优先**：能独立成文件的逻辑，绝不塞进上游文件
2. **注册函数**：IPC 收敛成 `registerXxxIpc()` 独立文件
3. **换皮脚本化**：品牌替换用脚本批量做，不手动逐个改
4. **维护 SOP**（固化在 `genoffice-rebrand` skill）：
   - 增量同步：`git fetch upstream && git merge`
   - 推倒重来：备份分支 → `reset --hard upstream/main` → 重新换皮

### 换皮（已固化进 genoffice-rebrand skill）

关键坑（已记录）：
- `Genspark` 不能粗暴替换成「AI 助手」——只有 `Genspark AI` 才是「AI 助手」，其余（账号/积分/登录）要「去掉」
- URL 替换会误伤 API 域名（`genspark.ai` → 空 导致 `https://www.` 坏 URL、`t.includes('')` 永远真）——已修复并记录
- force push 回退会丢自己加的文件（`release.yml`、`REBRAND.md`），需从备份分支恢复

### 构建与代理

- GitHub Actions 构建（`release.yml`，workflow_dispatch，文件名触发）
- 代理 `127.0.0.1:7897`（操作前先确认代理在跑）
- 下载 artifact 必验字节数
- 换官方 DeepSeek API（`api.deepseek.com/v1`，模型 `deepseek-v4-flash` 快速版）——之前踩坑的自建中转站 `ai-center.dslyy.com` 有「UTF-8 中文编码 bug」，已弃用

---

## 四、核心功能规划

### 4.1 模板套用（用户打开自己的模板）

```
用户打开自己的模板 .pptx → AI 识别版式角色（封面/内容/结束）
→ 按内容自动增页 → 克隆版式页（duplicateSlide + clearText）+ 填内容
→ logo/背景/版式/配色全程保留
```

- 技术底座：`duplicateSlide`（克隆版式）+ `clearText`（清文字）——**原生就有**
- 我们要做的：智能封装（自动识别版式角色 + 自动填内容），不是简单「填文字」

### 4.2 设计系统（版式库 + 设计规范 + 质检）

- 版式库：内容类型（封面/目录/章节/要点/对比/数据/人物/金句/总结）× 每种 2-4 个版式变体
- 落地：版式做成「HTML 模板」（样式写死），AI 只「选模板 + 填内容」
- 先聚焦「商务提案」场景（最通用、付费意愿强）
- 会员专享（免费用户只能「填文字」，会员解锁「专业版式库」）

### 4.3 模板库（网站，服务器下发）

**UI 放网站，桌面端只留「入口 + 导入」**（最小改动，避免上游更新冲突）：

```
桌面端「模板库」入口 → 打开网站模板页 → 用户挑模板
→ 点「用 UToOffice 打开」（deep link）→ 桌面端下载 .pptx + 打开 + 套用
```

- 模板库 UI/内容/分类/预览全在网站（改网站零发版）
- 桌面端只加：1 个入口按钮 + 1 个导入 handler

### 4.4 分销系统（网站端专区）

- 分销商注册 → 专属推广链接/码
- 用户通过分销链接购买 → 分销商拿佣金（**20%**）
- 网站后台：分销商查看推广数据、佣金

### 4.5 托管模型（后期，有流量再做）

- 非技术用户用「我们绑定的默认模型」
- 合规：需跟 DeepSeek 官方谈代理/分销资质，或找有授权的正规一级代理
- 预付制（用户先充值再用），避免垫资

---

## 五、UI 改造清单

### 5.1 账户区域（改动最小原则）

**账户按钮（首页左下角）**：
| 原 | 改成 |
|---|---|
| 头像（邮箱首字母） | 会员徽章 |
| 名字（邮箱前缀） | 免费→「开通会员」，会员→「永久会员」/「会员·到期」 |
| hover（邮箱） | 「查看会员权益」 |

**账户页（设置 → 账户）**：
```
┌─ 账户 ────────────────────────────┐
│  会员状态          免费版           │  ← 邮箱→会员状态
│  激活码          [____] [激活]     │  ← 新增
│  ┌─ 会员权益 ────────────────┐    │
│  │ ✓ 全部专业模板免费使用      │    │
│  │ ✓ 套用模板一键生成          │    │
│  │ ✓ 专业版式库+设计规范       │    │
│  └──────────────────────────┘    │
│                [ 开通会员 ]       │  ← 登录按钮→开通会员（挂卡密链接）
└──────────────────────────────────┘
```
- 激活成功后：会员状态刷新，激活码窗口显示「已激活」
- 「开通会员」点击 → 系统浏览器打开卡密购买链接（占位 `example.com/buy`）

### 5.2 GitHub star → 加入我们

- 文案「喜欢 GenOffice 吗？给个 Star」→「加入我们，获取最新模板和教程」
- 链接从 github.com → 指向我们的网站/社群

### 5.3 云端项目 → 模板库入口

- 「云端项目」是 Genspark 网页端项目同步（纯残留，删掉）
- 入口让位给「模板库」（指向我们网站）

---

## 六、卡密系统设计

### 架构（用户澄清：自建的是「卡密管理系统」，卖卡用「第三方发卡平台」）

```
┌─ 自建卡密管理系统（服务器）──────────────┐
│ ① 批量生成卡密                           │
│ ② 卡密验证/激活 API（新增或已有）          │
│    → 验证有效性 → 记录激活时间/设备 → 返回到期 │
└──────────────────────────────────────┘
              ↑ 激活时调 API
┌─ 第三方发卡平台（收款/销售）──────────────┐
│ 用户付款 → 发货（把卡密给用户）            │
│ 套餐链接挂第三方平台的购买页               │
└──────────────────────────────────────┘

┌─ UToOffice（桌面端）────────────────────┐
│ 输入卡密 → 调自建系统验证 API            │
│ → 成功：本地存会员状态（含到期时间）       │
│ → 换电脑：重新激活，平台认得卡 → 查重      │
└──────────────────────────────────────┘
```

### 必须在线验证（不能纯离线）

纯离线卡密的缺陷：换电脑丢激活状态、无法防「一卡多用」、年卡无法从激活日计时。

在线验证解决：换电脑（平台认得卡）、防共享（查重）、激活计时（激活日 + 有效期）。

### 待确认

- 自建卡密管理系统的源码位置、技术栈、现有功能、数据库结构
- 服务器配置（地址、数据库类型）

---

## 七、当前状态

### 已完成

- ✅ 干净重来：基于上游 `360ce06` 重新换皮，验证通过
- ✅ 修复换皮误伤（genspark.ai API 域名、t.includes('') 等 15+ 处）
- ✅ 换官方 DeepSeek API（解决中文生成 PPT）
- ✅ 换皮 skill 更新（genoffice-rebrand：Genspark 精确规则、URL 误伤、推倒重来流程）
- ✅ 备份分支 `backup/phase1-work`（Phase 1 旧工作，GitHub 有备份）

### 待办（按优先级）

1. **卡密系统关联**（先看自建卡密管理系统源码 → 设计验证 API）
2. **账户区域改造**（会员状态 + 激活码 + 开通会员 + 权益面板）
3. **设计系统**（版式库 → 设计规范 → 质检）
4. **模板库网站 + 桌面端入口**
5. **分销系统（网站）**
6. **GitHub star → 加入我们**
7. **云端项目 → 模板库入口**

---

## 八、关键决策记录

| 决策 | 结论 |
|---|---|
| 变现模式 | 卖 know-how（设计系统），不转售 API |
| 会员体系 | 卡密激活，不做账号登录 |
| 托管模型 | 后期做，先解决合规 + 预付制 |
| 模板库 | 放网站（服务器下发），桌面端只留入口+导入 |
| 设计系统 | 版式做 HTML 模板（样式写死），AI 只选模板填内容 |
| 卡密验证 | 在线验证（防共享、激活计时、换电脑） |
| 分销佣金 | 20% |
| 先聚焦场景 | 商务提案 |

---

## 附：关键文件位置

- 换皮 skill：`genoffice-rebrand`（Hermes skills/devops/）
- 项目换皮清单：`REBRAND.md`（项目根目录）
- 备份分支：`backup/phase1-work`（Phase 1 旧工作）
- 构建 workflow：`.github/workflows/release.yml`
- 上游 remote：`upstream`（genspark-ai/genoffice）
