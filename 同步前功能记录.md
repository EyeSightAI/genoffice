# UToOffice 自定义功能记录（同步上游前备份）

> 用途：2026-09-09 同步上游（v0.9.431 大更新，含 HTML 编辑器 + docs AI 重构）时，按用户要求「冲突以原版为主」，我们的功能先记录、以后看使用情况再决定是否恢复。
> 本清单 = 恢复功能的唯一依据，共 51 个自定义 commit。

---

## 一、换皮（品牌）—— 必须保留，同步后重新做

| commit | 内容 |
|---|---|
| 7ea8c62 | rebrand GenOffice → UToOffice（产品名/appId/图标/i18n/URL）|
| 5848b0b | 替换 *.html 窗口标题 GenOffice → UToOffice |
| 95805a0 | 恢复 genspark.ai API 域名（登录 baseUrl/LLM 代理/GENSPARK_ORIGIN）|
| e6472f0 | 恢复字体别名（GenOffice Batang/Myungjo/... 白名单）|

⚠️ 同步后必须重跑「换皮验收」（REBRAND.md 第六章 5 组 grep），且新增的 apps/html 模块也要换皮。

---

## 二、会员系统（卡密）—— 核心资产，必须恢复

| commit | 内容 |
|---|---|
| 3200b65 | 卡密激活 + 会员 UI（account pane 重做）|
| fdae9b1 | 钻石会员图标 |
| af87a2b | 在线激活 + 套餐购买流程 |
| 2f8665a | 套餐列表折叠 |
| bbca922 | 拦截非会员下载网站模板（deep link）|
| c2875f4 | 禁用匿名统计（GA4 默认关）|

涉及文件：membership.ts、home-api.ts、preload、index.ts、Home.tsx、SettingsModal.tsx、github-menu.ts。

---

## 三、模板库 + AI 套用 —— 核心护城河，必须恢复

| commit | 内容 |
|---|---|
| 47b8333 | 移除云端项目 + deep link + 「使用当前模板」标签 |
| 3dceaff | 强化「使用当前模板」约束 |
| 8e773b5 | 「更多」跳真实模板库 + 强制质检 |
| 6d7a3e8 | AI 自动选模板（search_templates + open_template）|
| a983651 | 模板加载到当前标签页（不脱节）|
| 7a49edd | 「使用模板库」标签 + 会员权限收紧 |
| 111a4c1 | 模板库模式禁止 generate_deck 绕过 |
| 30b3a08 | 跳过问卷（后被 2fa9496 纠正）|
| 2fa9496 | 问卷只问内容不问风格 |
| d8be70f | 模板元数据页数+版式结构 |
| bdfb0e7 | 561 套模板标签重打（23 规范词表）|
| d467313 | deep link 唤起优化（去重+缓存+聚焦）|

涉及文件：templates-meta.json、slides-skill.ts、AiPanel.tsx、slides-main.ts、ipc.ts、preload。

---

## 四、AI 体验修复（防止死循环/误导）—— 建议恢复

| commit | 内容 |
|---|---|
| 297e90e | 强制中文输出 |
| 1c561fb | 删页工具名纠正（delete_slide → apply_ops deleteSlide）|
| b499d49 | read_slide 返回背景色（修背景色死循环）|
| 1ea4a32 | 去掉 Genspark「登录」按钮 |
| 5ff09fc | 登录 → 「配置大模型」文字提示 |
| 007c7af | 补全 Markdown/PDF 提示 + 法语撇号修复 |
| 4e20368 | 错误文案「未登录」改「配置大模型」|
| 30c8ec8 | 去掉重复提示 |
| 879e592 | 删页死循环（一次批量删 + 从大到小）|

⚠️ 这些是「防止 AI 死循环/误导」的关键修复，同步后如果上游 AI 层重构（commands→ops），这些约束要重新适配。

---

## 五、更新机制 —— 必须恢复

| commit | 内容 |
|---|---|
| 9ee7754 | 更新源 GitHub Releases |
| 957c31b | 更新源服务器镜像（generic provider http）|
| 5829be9 | 发版自动同步服务器 |
| 37c8b78 | 版本 0.9.1 |

涉及文件：electron-builder.cjs、updater.ts、release.yml。

---

## 六、CI/构建 —— 必须恢复（否则构建失败）

| commit | 内容 |
|---|---|
| 004e35c | restore release.yml |
| c5ace70 | 去掉 --publish always |
| f61db45 | 安装包固定文件名 UToOffice-Setup.exe |
| 80f2516 | softprops/action-gh-release 手动发布 |
| be1f3d4 | xlsx-sidecar msvc target |
| 3e660ea | WIN_SIDECAR host target 目录 |
| a0acf76 | paramiko 同步服务器 |
| 7c9bd94 | print 英文避免编码错 |

---

## 七、分销 —— 建议恢复

| commit | 内容 |
|---|---|
| b7a6599 | 「加入我们」指向分销页 |

---

## 八、其他

| commit | 内容 |
|---|---|
| 4ccac21 | 去掉「点 Star」弹窗 |
| a5a52e9 | 白屏修复（onSetSpeakerNotes 残留）|
| f3d97b4 | restore REBRAND.md |
| 71ab95b | PROJECT_PLAN.md |
| a225a4b | membership 标记 |
| d8b734d | 版本 0.9.2 |
| bcbd88a | merge 上游 v0.9.0 |

---

## 恢复优先级（同步后按此顺序）

1. **P0（必须，否则不是 UToOffice）**：换皮、CI/构建、更新机制
2. **P1（核心资产，尽快恢复）**：会员系统、模板库+AI 套用
3. **P2（体验，建议恢复）**：AI 修复、分销、去 Star、白屏修复
4. **P3（文档，无所谓）**：PROJECT_PLAN.md、REBRAND.md 等

---

## 同步操作记录

- 同步前版本：v0.9.11（本地 main）
- 同步目标：upstream/main（v0.9.431，含 HTML 编辑器 + docs AI 重构）
- 冲突策略：以原版（上游）为主，功能记录后按使用情况恢复
